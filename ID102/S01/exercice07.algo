algorithme NomAlgorithme

variables
    t,h,m,s : entier

debut
    // Entrée
    ecrire("donner le nombre de secondes : ")
    lire(t)
    
    // Traitement
    h = t div 3600
    t = t mod 3600
    m = t div 60
    s = t mod 60
    ecrireln(h,":",m,":",s)

fin